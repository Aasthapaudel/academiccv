{
  "name": "nuxt-app",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
     "build": "nuxt generate",
  "deploy": "npm run build && cd .output/public && git init && git checkout -b gh-pages && git add . && git commit -m 'Deploy Nuxt site' && git remote add origin git@github.com:Aasthapaudel/academiccv.git && git push origin gh-pages --force"  ,
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "^4.4.2",
    "vue": "^3.5.30",
    "vue-router": "^5.0.3"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  
}
}
